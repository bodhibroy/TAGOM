// This function decided whether the user has given a correct answer
// or not

function evaluateUser(user_val)
{
    if(window.flag==user_val)
    {
	alert("Correct!");
    }
    else
    {
	alert("Wrong!");
    }
}